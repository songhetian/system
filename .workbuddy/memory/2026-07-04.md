# 2026-07-04

## 审计纠偏

grill-with-docs 全面审计发现上一份审计报告严重误判：
- RBAC 权限：12/12 路由全部已实现（内联 addHook 模式）
- 字段脱敏：employee 路由已通过 toListItem/toFullDetail 实现
- 审批流串联：leave.service 创建请假时已自动启动 workflow
- 行级过滤：leave 等路由已使用 buildRowAccessFilter
- 迟到早退：attendance-record.service 已对比班次自动计算
- WebSocket：已有 ping/pong + 断线清理
- 系统实际完成度约 90%（非 55%）

## 后端 Stub 修复

expense/kb/overtime 三个模块的硬编码 userId 修复：
- `expense.service.ts`：移除 `employeeId = 1` 默认值
- `kb.service.ts`：移除 `uploaderId = 1`、`uploaderName = 'admin'` 默认值，路由传 `request.user.id/username`
- `overtime.service.ts`：`createOvertimeRequest` 先创建请求再建审批实例，`sourceId: 0` → 真实 ID，查 employee.name 填 applicantName；`rejectOvertimeRequest` 持久化 reason

## 前端修复

- P0：`useAttendance.ts` 修复 `useQueryClient()` 从模块级移到函数体内（React Hook 规则违反）
- P1：4 页分页全部修复 — leave、attendance、approval、schedule 的 `onChange: () => {}` → 真实状态管理
- leave：`STATUS_MAP` 颜色 `'orang'` → `'orange'`
- 健康检查：`/health` 端点 + `middleware/auth.ts` 白名单

## 当前状态

- 后端：274 测试全绿，32 测试文件
- 后端 9 个 stub 中 4 个已修复，剩余 5 个为刻意延后的 ponytail 债务
- 前端：7 个占位页面 + 1 个半成品 Dashboard 尚未实现

## 前端页面建设

完成 2 个页面从占位到可用：

**员工管理** (`features/employee/`)
- `hooks/useEmployee.ts`：useEmployeeList/useCreateEmployee/useUpdateEmployee/useDeleteEmployee + DepartmentTree/PositionList
- `index.tsx`：搜索栏（关键词+部门+状态筛选）、BaseTable 员工列表（分页/序号/操作列）、BaseFormModal 创建/编辑表单

**组织架构** (`features/organization/`)
- `hooks/useOrganization.ts`：Department/Position/Rank 全套 CRUD hooks
- `index.tsx`：Tabs 分部门/岗位/职级三个子页，部门树缩进显示，均有删除确认

## Dashboard 接入真实数据

- 4 个 Statistic 从硬编码改为 API：员工总数(`/employees`)、今日排班(`/schedule/schedules`)、本月出勤(`/attendance/attendance-summaries`)、待审批(`/workflow/instances`)
- 快捷入口改为可点击导航（useNavigate）
- 系统公告从 API 获取最近公告
- 样式新增：quickLink(蓝色可点)、announcement(分隔线)、dimText(灰色空态)

## 今日总结

- 后端：274 测试全绿，stub 修复 4 个
- 前端：7 个占位页面→0，Dashboard 硬编码→真实 API
- 12 个功能页面全部可用
